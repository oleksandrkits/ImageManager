%h1 Here is your new image
= image_tag @url
%br/
= link_to 'Add image', new_image_path({:url => @url})
= link_to 'Get new image', root_path
= link_to 'User list', users_path
%h1 Here is list of all your images
- @images.each do |image|
  = image_tag image.url, class: "img-thumbnail"
  = link_to 'Delete', image, method: :delete, data: { confirm: 'Are you sure?' }
  %br/